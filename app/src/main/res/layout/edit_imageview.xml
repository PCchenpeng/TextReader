<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

    <com.dace.textreader.view.editor.DataImageView
        android:id="@+id/edit_imageView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scaleType="fitXY"/>

    <ImageView
        android:id="@+id/iv_delete"
        android:layout_width="25dp"
        android:layout_height="25dp"
        android:layout_alignRight="@id/edit_imageView"
        android:layout_alignTop="@id/edit_imageView"
        android:layout_margin="10dp"
        android:scaleType="fitXY"
        android:src="@drawable/icon_image_delete"/>

    <ImageView
        android:id="@+id/iv_change"
        android:layout_width="56dp"
        android:layout_height="25dp"
        android:layout_alignBottom="@id/edit_imageView"
        android:layout_alignLeft="@id/edit_imageView"
        android:layout_margin="10dp"
        android:scaleType="fitXY"
        android:src="@drawable/icon_image_change"/>

</RelativeLayout>