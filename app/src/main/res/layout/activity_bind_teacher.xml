<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:background="@color/colorOrderDetailBg"
              android:orientation="vertical">

    <include layout="@layout/top_back_title_layout"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:background="@color/colorWhite"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingLeft="15dp"
        android:paddingRight="15dp">

        <EditText
            android:id="@+id/et_code_bind_teacher"
            android:layout_width="0dp"
            android:layout_height="44dp"
            android:layout_weight="1"
            android:background="@drawable/shape_coupon_edit_bg"
            android:ellipsize="end"
            android:gravity="center_vertical"
            android:hint="请输入老师编码，进行绑定提交作业"
            android:maxLines="1"
            android:paddingLeft="22dp"
            android:paddingRight="22dp"
            android:textSize="14sp"/>

        <TextView
            android:id="@+id/tv_code_bind_teacher"
            android:layout_width="90dp"
            android:layout_height="44dp"
            android:background="@drawable/shape_coupon_text_bg"
            android:gravity="center"
            android:text="立即绑定"
            android:textColor="@color/colorWhite"
            android:textSize="15sp"
            android:textStyle="bold"/>

    </LinearLayout>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recycler_view_bind_teacher"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"/>

</LinearLayout>